apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.global.namingPrefix }}-ingress-dev
  namespace: dev
spec:
  rules:
    - host: aleph.devel
      http:
        paths:
          - path: /
            backend:
              serviceName: {{ .Values.global.namingPrefix }}-ui
              servicePort: 80
          - path: /api
            backend:
              serviceName: {{ .Values.global.namingPrefix }}-api
              servicePort: 8000
